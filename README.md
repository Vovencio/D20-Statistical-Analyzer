# D20 Statistical Analyzer

A cheat-detection tool for tabletop dice rolls. For each player, it computes the binomial tail probability for every die face given the total number of rolls, then flags the most anomalous result against a configurable significance threshold. Supports saving and loading player histories.
